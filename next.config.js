/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "images.unsplash.com",
      "www.pinterest.com",
    ],
  },
};

module.exports = nextConfig;
